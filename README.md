# Archive: Parser Prototype

This branch archives the old parser script. The Flask app uses app/parser_gemini.py
It is **not** used by the Flask backend.

- **Archived file:** `gemini-start.py`
- **Status:** Historical reference only
- Do not add secrets or `.env` here.
- Main development continues on the `backend` and `testing-main` branches.
